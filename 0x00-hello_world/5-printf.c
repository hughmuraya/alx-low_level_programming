#include <stdio.h>

/**
 * Main- prints line provided
 * Return- exits correctly if 0 else non-zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
